on:
    workflow_call:
  
jobs:
    test:
        runs-on: ubuntu-latest
  
        steps:
            - uses: actions/checkout@v4
            - name: Test
              run: dotnet test --verbosity normal